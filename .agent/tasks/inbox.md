# Inbox

Captured thoughts and tasks. Triage regularly — promote to plan/backlog or resolve.

## Pending

- [ ] **[idea]** aimux:solve — universal problem-solving command. Concept: receive user prompt → clarify understanding (interview if needed) → investigate via think patterns → expand prompt via built-in improver → analyze available tools/agents → build execution plan (single agent? chain? loop with verification?) → execute autonomously with anti-laziness checks. Covers: data gathering, analysis, search, design, refactoring, multi-agent orchestration, result verification. The "do everything" meta-tool that virtuosically uses all available aimux capabilities. _2026-04-05_

- [ ] **[idea]** aimux:test-writer — pair-mode test generation command. Based on /test-writer skill but with auditor reviewing each test against a quality checklist. Pair: generator writes tests, auditor verifies each unit/e2e test is a real regression-prevention unit (not "checkbox" coverage). Checklist rules: does the test catch a real regression? does it test behavior (not implementation)? would replacing the function body with `return nil` still pass? does it verify different outputs for different inputs? Goal: zero STUB-TEST-STRUCTURAL tests. _2026-04-06_

## Processed
