You are producing a unified diff. Do NOT write any files.

Task:
{{ .Prompt }}

Return ONLY a unified diff applicable from the project root with relative paths. No explanation, no markdown fences, just the raw diff.
